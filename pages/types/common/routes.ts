// export class Route {
//   origin?: string;
//   destination?: string;
  
//   constructor(origin?: string, destination?: string) {
//     this.origin = origin;
//     this.destination = destination;
//   }
// };

export class Route {
  origin: string;
  destination: string;

  constructor(origin: string, destination: string) {
    this.origin = origin;
    this.destination = destination;
  }
}
